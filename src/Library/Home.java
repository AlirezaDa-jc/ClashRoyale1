package Library;

/**
 * @author Alireza.d.a
 */
public class Home extends Entity{
    public Home(int x, int y, Grid grid, EntityType type, String imagePath,String player) {
        super(x, y, grid, type, player, imagePath,0,0,0);
    }
}
